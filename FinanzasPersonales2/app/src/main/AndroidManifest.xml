<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="finanzas_personales.uptc.edu.finanzaspersonales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registro" />
        <activity android:name=".RegistroCategoriasCapital"
            android:label="@string/titulo_config"/>
        <activity android:name=".Lector_Preferencias" />
        <activity
            android:name=".PrincipalIngresosGastos"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Listar_Gastos" />
        <activity android:name=".Listar_Ingresos" />
        <activity android:name=".AdministrarCategorias" android:label="@string/titulo_ges"/>
    </application>

</manifest>